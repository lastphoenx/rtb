# RTB rsync excludes — eine Zeile pro Pattern (rsync --exclude-from)
# Produktion: /opt/apps/rtb/excludes.txt (RTB_EXCL)

# Temporäre / Cache-Verzeichnisse
.cache/
tmp/
*.tmp

# Restore-/Download-Pfad (Backup-Loop vermeiden)
/restore/

# macOS AppleDouble-Metadaten (nicht sichern — reduziert Pool/pCloud-Noise)
**/._*

# pCloud-Pipeline unter /srv/nas (mergerfs-Doppel, NICHT /srv/pcloud-* auf SSD2):
# Von RTB-Quelle ausgeschlossen → triggert kein Backup bei jedem Upload.
# Vor jedem RTB-Lauf: sync_pipeline_export.sh kopiert Live-Daten nach
# /srv/nas/Backup/raspi5nas/pcloud-{archive,temp}/ (ohne fuehrenden / nur Top-Level!).
/pcloud-archive/
/pcloud-temp/
