# RTB rsync excludes — eine Zeile pro Pattern (rsync --exclude-from)
# Produktion: /opt/apps/rtb/excludes.txt (RTB_EXCL)
#
# === ZWEI SCHICHTEN ===
#
# A) DIESE DATEI (excludes.txt) — echtes Backup (rsync_tmbackup)
#    Pattern hier = wird NIE ins RTB-Snapshot kopiert.
#
# B) rtb_check_excludes.sh — NUR Delta-Check (rsync -ni, „triggert Backup?“)
#    Zusätzlich: /pcloud-archive/, /pcloud-temp/, /Backup/pbs2/, /Backup/pve2/
#    → triggern kein Backup allein, landen aber im Snapshot wenn z.B. Paperless triggert.
#
# Siehe rtb/README.md § Excludes & Backup Loop Prevention

# Temporäre / Cache-Verzeichnisse
.cache/
tmp/
*.tmp

# Python — nie sichern (auch nicht bei Mitbackup)
__pycache__/
*.py[cod]
*$py.class
.Python
*.egg-info/
.mypy_cache/
.pytest_cache/
.ruff_cache/
.venv/
venv/

# Restore-/Download-Pfad (Backup-Loop vermeiden)
/restore/

# pCloud-Pipeline: JSON-Upload-Scratch (pro pCloud-folderid ein Temp-Ordner, ~47k leere Hüllen).
# Kein Nutzdaten-Bestand; kanonisch unter /srv/pcloud-archive/staging/ (nicht im RTB-Baum).
# Resume-State liegt unter /srv/pcloud-archive/resume/ (bleibt auf SSD2, wird nicht RTB-gesichert).
/pcloud-archive/staging/

# macOS AppleDouble-Metadaten (nicht sichern — reduziert Pool/pCloud-Noise)
**/._*

# PBS2 runtime state on NAS (ephemeral; GC/lock changes every cycle)
/Backup/pbs2/.lock
/Backup/pbs2/.gc-status
