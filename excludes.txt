# RTB rsync excludes — eine Zeile pro Pattern (rsync --exclude-from)
# Produktion: /opt/apps/rtb/excludes.txt (RTB_EXCL)
#
# === ZWEI SCHICHTEN ===
#
# A) DIESE DATEI (excludes.txt) — echtes Backup (rsync_tmbackup)
#    Pattern hier = wird NIE ins RTB-Snapshot kopiert.
#    Inkl. /pcloud-archive/, /pcloud-temp/ (Pipeline, RAM/Rsync).
#
# B) rtb_check_excludes.sh — NUR Delta-Check / Signature-Trigger
#    Dieselben Pipeline-Pfade: Änderungen dort starten KEIN Backup allein.
#
# Siehe rtb/README.md § Excludes & Backup Loop Prevention

# Temporäre / Cache-Verzeichnisse
.cache/
tmp/
*.tmp

# Python — nie sichern (auch nicht bei Mitbackup)
__pycache__/
*.py[cod]
*$py.class
.Python
*.egg-info/
.mypy_cache/
.pytest_cache/
.ruff_cache/
.venv/
venv/

# Restore-/Download-Pfad (Backup-Loop vermeiden)
/restore/

# Pipeline-Artefakte auf mergerfs — nicht ins RTB (zu viele Dateien → rsync-RAM;
# Manifeste liegen auf pCloud; triggern ohnehin kein Backup allein, s. rtb_check_excludes.sh)
/pcloud-archive/
/pcloud-temp/

# macOS AppleDouble-Metadaten (nicht sichern — reduziert Pool/pCloud-Noise)
**/._*
